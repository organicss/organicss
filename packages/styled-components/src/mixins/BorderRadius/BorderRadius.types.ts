import { DefaultValue } from '../../types/global';

export type BorderRadiusProps = {
    bottomLeft?: DefaultValue;
    bottomRight?: DefaultValue;
    radius?: DefaultValue;
    topLeft?: DefaultValue;
    topRight?: DefaultValue;
};
